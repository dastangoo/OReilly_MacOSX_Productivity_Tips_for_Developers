```
apt search lua
apt search lua | grep ^lua
apt show lua
apt install lua
which lua
nmap --script-help "http*"
nmap --script <script>
cd /usr/share/nmap/scripts
lua <script>.nse 
cd ~/bin
touch testnodejs
---
#!/usr/bin/env node
console.log('Hello');
---
chmod +x testnodejs
ln -s /Users/<user>/bin /usr/local/bin
which testnodejs
testnodejs
```
