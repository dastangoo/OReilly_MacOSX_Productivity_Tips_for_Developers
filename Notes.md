```bash
pushd /usr/bin
popd
cd /usr/bin
cd -
brew search z
brew info z
wget <z script>
vim ~/.zshrc
vim ~/.bashrc
z Down  
defaults read com.apple.finder 
defaults write com.apple.finder AppleShowAllFiles YES
```
